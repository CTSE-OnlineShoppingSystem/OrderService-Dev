# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - CTSE-Microservice

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://ctsemicroservice.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_1cbf341ddff54496835a47b99e8d2efc }}
        password: ${{ secrets.AzureAppService_ContainerPassword_64c57735d5b84ae593092306fd6fc8a1 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: ctsemicroservice.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_1cbf341ddff54496835a47b99e8d2efc }}/order-service-dev:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'CTSE-Microservice'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_924608e6a90a423fab88ccea91ff60b5 }}
        images: 'ctsemicroservice.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_1cbf341ddff54496835a47b99e8d2efc }}/order-service-dev:${{ github.sha }}'